<%= form_for(@certificate_type) do |f| %>
  <% if @certificate_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@certificate_type.errors.count, "error") %> prohibited this certificate_type from being saved:</h2>

      <ul>
      <% @certificate_type.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :type %><br />
    <%= f.text_field :type %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
